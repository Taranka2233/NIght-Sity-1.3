rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Media uploads are disabled until encrypted blob storage is enabled by
    // the client. This prevents accidental plaintext uploads.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
